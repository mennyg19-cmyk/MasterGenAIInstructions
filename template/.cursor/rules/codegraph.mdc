---
description: CodeGraph MCP -- deterministic structural code intelligence. Always on when MCP configured. Hybrid with Read/grep; parent backbone + subagent drill-down.
alwaysApply: true
---

# CodeGraph (always on when MCP available)

Adapted from [codegraph](https://github.com/colbymchenry/codegraph) (MIT). **Deterministic** -- tree-sitter AST + SQLite index, not LLM summaries. Same query + same index state = **same result regardless of which model runs it.** Multi-model value is in *interpretation* and *which follow-up queries to run*, not in re-reading files.

Requires: `codegraph install` (MCP wiring) + per-project `codegraph init` (builds `.codegraph/`). If `.codegraph/` missing and CLI is available, **run `codegraph init` yourself** -- don't only ask.

## Hybrid lookup (Rule Preference)

| Need | Tool |
|---|---|
| Structure: where is X, who calls Y, impact, trace, signatures | `codegraph_*` when index exists |
| Literals: strings, comments, log messages, configs, markdown | Read / grep |
| Index missing / stale banner lists file | Read that file; re-sync handles the rest |
| Old reference app at another path (rebuild) | `codegraph init` there too if CLI available; else Read |

**Do not grep-for-symbol** when `codegraph_search` works. **Do not re-verify graph results with grep** -- trust AST output; check staleness banner instead.

## Tool map

| Question | Tool |
|---|---|
| Find symbol X | `codegraph_search` |
| Project / area file tree | `codegraph_files` |
| What calls Y? | `codegraph_callers` |
| What does Y call? | `codegraph_callees` |
| Flow X → Y | `codegraph_trace` |
| Blast radius if I change Z | `codegraph_impact` |
| Signature / source | `codegraph_node` or `codegraph_explore` |
| Task area context | `codegraph_context` |
| Read indexed file (Read parity) | `codegraph_node` with `file` only |
| Index health | `codegraph_status` |

**Before rename / delete / signature change / refactor command:** run `codegraph_impact` first (Rule Preference: mandatory).

## Parent + subagent pattern (A+B hybrid)

**CodeGraph output does not vary by model** -- parent pre-fetch is the shared factual backbone; subagents add *different-model judgment* on top.

1. **Parent (orchestrator)** runs the baseline scan, writes per-area digests to `rebuild-audit/graph-backbone/` (routes, symbols, entry points -- facts only).
2. **Each subagent** starts with `codegraph_status` (or equivalent MCP check):
   - **MCP works:** use `codegraph_*` for drill-down; do NOT re-Read the whole tree. Still write its own audit (interpretation differs by model).
   - **MCP unavailable:** read parent's digest files (proof-of-read required per `subagents.mdc`); ask parent to run any missing `codegraph_*` query and append to the digest.
3. Parent never blocks on subagent MCP failure -- fill gaps with parent-run codegraph, then re-spawn if needed.

## Rebuild reference app (Rule Preference)

When REBUILD-PLAN.md names an old reference path: offer/run `codegraph init` in **this repo and the reference path** (two indexes). Feature harvesting from old code uses that graph + Read for non-indexed assets.

## vs ponytail / your rules

- **Allies:** fewer Read/grep loops = fewer tokens (ponytail + codegraph align).
- **Conflicts:** follow ponytail conflict protocol; default protocol-safe, tell user, offer README § Rule Preference entry.
- **Walkthroughs off:** codegraph replaces file-navigation overhead; does not replace DECISION-LOG, expectation files, or review gates.

## Not initialized

If tools return "not initialized": run `codegraph init` (install CLI first if missing). Bootstrap/apply offers init on new projects when CLI is present.
